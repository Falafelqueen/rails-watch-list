<div class="nav-bar">
  <img src="" alt="" class="logo">
  <h1></h1>
  <div class="search-form"></div>
</div>
  <div class="banner">
    <div class="container">
    <h1>My lists</h1>
  </div>
  </div>
  <div class="container">
  <div class="card-holder">
    <% @lists.each do |list| %>
      <%= link_to list_path(list) do %>
    <div class="card">
      <%= cl_image_tag list.photo.key, height: 300, width: 300, crop: :fill%>
      <h2><%= list.name %></h2>
    </div>
    <% end %>
      <% end %>
  </div>
  <button class="btn btn-new"><%= link_to "Create new list", new_list_path %></button>
</div>
